{% extends 'base.html.twig' %}

{% block title %}Event New{% endblock %}

{% block body %}
<div class="container-fluid">

<h1>Event creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('event_index') }}">Back to the list</a>
        </li>
    </ul>
    </div>
{% endblock %}
